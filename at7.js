let X = parseInt(prompt("Qual seria o valor da variavel? "))
let resultado
if(X % 2 == 0) {
  resultado = X + 5
    alert(`A variavel ${X} é par e o resultado é ${resultado}`)
  } else
   { resultado = X + 8
 alert(`A variavel ${X} é impar e o resultado é ${resultado}`)}
  